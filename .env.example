# docker-compose.yml tarafindan otomatik okunur (ayni dizinde `.env` olarak
# kopyalayin). Bu dosya SADECE ornek - gercek `.env` asla commit edilmez
# (bkz. .gitignore).
MYSQL_ROOT_PASSWORD=CHANGE_ME_BEFORE_DEPLOY
MYSQL_PASSWORD=CHANGE_ME_BEFORE_DEPLOY
